# Board
## DOING

## TODO
- [ ] T-168 [P1] saitranslate README/doc drift: bundle has TWO systems -- flat README_XX (v7.55) + subdirs XX/{README,RFC,SPEC,STYLE,CONTRIBUTING,...} (full doc-set, RFC_XX 72KB heavily drifted). Re-scan exact drift, update canonical system for 32 langs + Дед, priority EN/RU/EE/JA -- fabricate nothing (translate.md §2/§3). RUN AS A DEDICATED/PARALLEL saitranslate INSTANCE, not the main agent under limits | verify: 32+Дед current and spot-checked
- [ ] T-169 [P2] saitranslate reconcile: pick the canonical structure (flat README_XX vs the fuller subdirs XX/) and remove the other -- a real quality call, done inside the T-168 dedicated run | needs: T-168 | verify: single consistent structure in saitranslate/kitchen
- [ ] T-170 [P1] bare-name crew command discovery is unreliable, found live in FastPrompter crew test 2026-07-24, THREE compounding failures observed directly (FreeBuff/DeepSeek + OpenCode/DeepSeek, both weak free-tier models):
  (1) STALE PER-PROJECT COPY -- a project that spawned subs/ before v7.56.0 (crew) has a frozen PROTOCOL.md with no §7; `saipen sub spawn` never re-syncs an already-copied extension when home gains new vocab. Ad-hoc synced in FastPrompter only, not a general fix.
  (2) GLOBAL SKILL NEVER CARRIED extensions/subs/ AT ALL -- confirmed via FreeBuff's own local .freebuff/desktop.db (SQLite, readable: tables threads/messages/queue_items, real-time queryable): agent loaded skill "saipen", read the globally-injected RFC.md/STYLE.md/UI.md from ~/.agents/skills/saipen/, found ZERO mention of subSaipen roles anywhere, and reasoned "saipython" = a "saipen"+"python" portmanteau -- a plausible-sounding wrong guess, not a lookup failure. bootstrap/inject.sh + inject.ps1 copy_skill() never included extensions/subs/ in the distributed bundle. FIXED THIS SESSION: both injector scripts now copy extensions/subs/ (PROTOCOL.md/crew.md/README/TEMPLATE) into every platform's skill folder; tools/validate.py's dist_tokens check extended to catch a regression here. NOT YET applied to any already-installed live skill folder (~/.claude/skills, ~/.agents/skills, ~/.config/opencode/skills, etc.) -- re-running the injector on the user's actual machine is a separate, larger action requiring their go-ahead, deliberately not done automatically.
  (3) RFC §1.10's "check extension vocab before declining" (OpenCode/DeepSeek trace) requires an inferential leap (unknown word -> go find + open the right file) a weak model skipped, declining outright instead of looking.
  | verify: (a) confirm re-running the injector actually fixes a cold FreeBuff/OpenCode session's discovery of "saipython"/"saihunt" (re-test after user approves an injector re-run), (b) design a `saipen sub sync`-style refresh path for already-spawned per-project copies (failure 1), (c) make extension-vocab discovery mechanical not inferential in RFC §1.10 wording (failure 3) -- test against weak/free-tier models specifically, not just strong ones
  | found: a real live saihunt run (OpenCode/DeepSeek-free, once bare-name WAS understood) surfaced two MORE real spec gaps, both fixed this session: (4) `TEMPLATE/BOARD.md` shipped empty with zero example, and PROTOCOL.md §1 only said "same shape as Core" abstractly -- saihunt invented its own ad-hoc shape, reusing OUTBOX's bold-field markdown for its BOARD.md instead of RFC §1.2's checkbox ticket line; fixed with an explicit example in both files. (5) spawn instructions (§7) told an agent to replace `agent:`/`saipen_home:` but never said to also set `updated:` to a real timestamp -- FastPrompter's saihunt bumped only the DATE component of TEMPLATE's placeholder and left `00:00:00` (partial-placeholder edit, not a real checkpoint time); §7 now explicitly lists `updated:` too. Corrected the one live instance directly (cosmetic, findings/BOARD content untouched).
  | blocker: LIVE VERIFICATION PAUSED BY USER 2026-07-24 -- FreeBuff itself failing to start sessions ("Could not start a Freebuff session for deepseek/deepseek-v4-flash", not a SAIPEN-side bug); source fixes above already landed regardless. Resume live crew re-test once user confirms FreeBuff/OpenCode Zen are reliably reachable -- not moved to ## BLOCKED since nothing is stuck, just deliberately not being worked right now
## DONE
- [x] T-136 CLOSED (was P3 design-debt, 8 versions): MARKHUNT got a manifest-driven closure self-test in markhunt.md (vectors 5/5, cursor:done, head_end==HEAD, findings==[MARKHUNT] tickets) before DONE + enriched auditable completion line; CONFORMANCE row 37 + stub. No validate.py change (self-enforced, no ceremony)
- [x] T-167 saitranslate carve-out fix: removed 28 flat GUIDE_XX (duplicated hand-maintained guides/)
## BLOCKED
